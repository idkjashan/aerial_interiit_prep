# .agent

Notes for coding agents working on this repo. Not part of the submission.

- `HANDOVER.md`: current state, what is and isn't verified, and the task list for the
  Ubuntu machine. Start here.
- `environment.md`: snapshot of that machine (versions, paths, topics). Some PX4 values in
  it are wrong; the corrected ones are in `PART_1/docs/setup.md` §0.

Keep agent-only notes in this folder. Everything under `PART_1/` and `PART_2/` should read
as normal project documentation.
